# Required for real streaming LLM insights. Without this set, the backend
# falls back to a local deterministic stub streamer so the UI can still be
# demoed end-to-end.
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-sonnet-5

# Seconds between simulated sensor readings
SENSOR_INTERVAL=1.0
